/**
 * Published `@vanduo-oss/vd3` barrel counts used on marketing surfaces.
 *
 * Components: `export { default as Vd* }` in `dist/index.d.ts` (1.7.3).
 * Composables: `export * from "./composables/use*"` modules in that barrel
 * (not every `use*` function — `useToast` also exports `useToastStore`).
 */
export const VD3_COMPONENT_EXPORTS = 63;
export const VD3_COMPOSABLE_MODULES = 39;
