# Docs Oola Dock — Tasks

- [x] 1. Depend on published `@vanduo-oss/vd3` `^1.5.0`; bump site to `1.4.0`.
- [x] 1b. Refresh lockfile from npm (Safe-Chain / `minimum-release-age-exclude`
      for `@vanduo-oss/*`); no `link:../vd3`.
- [x] 2. Add `/components/dock` page, nav, router, keywords, e2e ROUTES, live
      customizer, ū brand, API table.
- [x] 3. Add home `#home-oola` scroll story above `#home-seemore`.
- [x] 4. Changelog card for vd3 1.5.0 (Latest badge).
- [x] 5. `pnpm typecheck`, lint, stylelint, format:check, test, build.
- [x] 6. Playwright visual-parity baselines (`--update-snapshots`) and a11y smoke.
- [x] 7. Re-baseline `MAX_GZIP_KB` after measuring published 1.5.0 + Oola
      (559.9 KB gz → 575 KB budget).
- [x] 8. Push and open the docs PR against `main`.
