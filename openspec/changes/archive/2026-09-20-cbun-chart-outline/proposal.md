# Delineate the CBUN chart preview

## Why

After removing the outer cards, the chart needs a subtle edge comparable to
the flowchart component's own boundary.

## What changes

Add a theme-aware 1px rounded border directly around the chart preview,
without restoring the duplicate heading or card padding. No dependencies change.
