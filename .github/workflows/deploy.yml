name: Deploy to GitHub Pages

# Builds the vite-ssg site and publishes dist/ to GitHub Pages at
# https://vd3.vanduo.dev/ (custom domain; Vite base `/`).
# Dogfoods published @vanduo-oss/vd3 from npm plus Labs sibling
# link:../../vdl/vdl-cbun — CI checks that tree out beside
# perspective/vd3-docs to match the committed path.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: perspective/vd3-docs
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: perspective/vd3-docs

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: vanduo-oss/vdl-cbun
          path: vdl/vdl-cbun

      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          version: 10.28.2

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            perspective/vd3-docs/pnpm-lock.yaml
            vdl/vdl-cbun/pnpm-lock.yaml

      - name: "Install and build link: sibling (vdl-cbun)"
        working-directory: ${{ github.workspace }}
        run: |
          echo "::group::Build vdl/vdl-cbun"
          pnpm install --frozen-lockfile --dir vdl/vdl-cbun
          pnpm run --dir vdl/vdl-cbun build
          echo "::endgroup::"

      - run: pnpm install --frozen-lockfile

      # ── Quality gates ──────────────────────────────────────────────────────
      # deploy.yml runs on push to main INDEPENDENTLY of ci.yml, so a type /
      # a11y / bundle-size regression could otherwise auto-publish before ci.yml
      # (which does not run these suites) ever fails. Gate them here so a broken
      # build cannot reach Pages. Custom domain vd3.vanduo.dev is served at the
      # site root, so one base-'/' build is both the gate artifact and the
      # Pages upload (a11y + size suites already assume base '/').
      - run: pnpm run typecheck

      - name: Install Chromium for the a11y smoke
        run: pnpm exec playwright install --with-deps chromium

      - name: Build for GitHub Pages (base '/')
        run: pnpm run build
      - run: pnpm run test:a11y
      - run: pnpm run test:size

      - name: Disable Jekyll processing
        run: touch dist/.nojekyll

      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: perspective/vd3-docs/dist

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
